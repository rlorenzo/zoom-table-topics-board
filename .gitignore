.venv/
__pycache__/

# Crash-recovery session file (see SESSION_FILE in board.py). Meeting-scoped
# and machine-local; a clean exit deletes it.
.board-session.json
.board-session.json.tmp
*.pyc
.DS_Store
.claude

# uv
.python-version

# pre-commit / tooling caches
.pre-commit-cache/
.ruff_cache/
.pytest_cache/
.mypy_cache/
.fallow/

# node (deps pinned in package.json + lockfile; node_modules is not committed)
node_modules/

# coverage
.coverage
htmlcov/
coverage/

# local browser-verification screenshots
.verify/

# agy (antigravity) scratch cache
cache/
